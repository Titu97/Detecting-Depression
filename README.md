The preprocessed data must be downloaded from [this link] and placed in the same directory as main.ipynb.

To install the required dependencies, use the following command:

pip install -r requirements.txt
